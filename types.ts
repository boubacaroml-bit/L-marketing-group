export interface ServiceItem {
  title: string;
  icon: React.ReactNode;
  items: string[];
}

export interface ContactInfo {
  name: string;
  role: string;
  phone: string;
  email: string;
  instagram: string;
}